<configuration>
[% cfgpath = [] %]
[% FOREACH pair IN CCM.contents.pairs %]
[%-     IF CCM.is_hash(pair.value) -%]
[%-         INCLUDE property.tt data=pair.value cfgpath=cfgpath.merge(pair.key) -%]
[%      END %]
[%  END %]
</configuration>


